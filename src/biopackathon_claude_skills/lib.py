def add(a: int, b: int) -> int:
    """Add two integers.

    Args:
        a (int): The first addend.
        b (int): The second addend.

    Returns:
        int: The sum of ``a`` and ``b``.
    """
    return a + b

